# Core dependencies
slack-bolt>=1.16.0
# Bump dotenv to >=1.0.0 to avoid downstream warnings
python-dotenv>=1.0.0
# Ensure compatible pairing (requests 2.32+ works with urllib3 2.x which removed vendored six; older requests versions expect urllib3.packages.six)
requests>=2.32.3
urllib3>=2.2.0,<3
nltk>=3.7

# Security and utilities
slack-sdk>=1.27.0  # For signature validation
cryptography>=41.0.0  # For secure token handling
